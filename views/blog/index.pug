extends ../layout

block content
  ul
    each item in post
      li(style="margin-bottom: 15px;")
        a(href=item.url style="font-size: 1.5em") #{item.title}
        | #[br] #{item.caption}

    else
      li error loading content
